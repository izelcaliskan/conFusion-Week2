import { HDirective } from './highlight.directive';

describe('HDirective', () => {
  it('should create an instance', () => {
    const directive = new HDirective();
    expect(directive).toBeTruthy();
  });
});
